
This is JEE to database using OO-relational mapping, ORM
 to convert between objects and database records. 
The Java Persistence API (JPA) is used to do the mapping.
Querydsl is used for querying

Purpose:
  Object relational mapping

Install:
    Open in NetBeans, build (to download Maven-dependencies)
    NOTE: Tomcat 8 accepts Java 7 (i.e. not Java 8) 

Run:
    NetBeans, Tomcat and MySQL

